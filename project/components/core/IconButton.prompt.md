Square icon-only button (44/52px). Pass a Lucide `<i data-lucide>` or `<svg>` child; always set `aria-label`.

```jsx
<IconButton aria-label="Relancer" variant="signal"><i data-lucide="dices" /></IconButton>
<IconButton aria-label="Réglages"><i data-lucide="sliders-horizontal" /></IconButton>
```
