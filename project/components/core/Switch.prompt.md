Boolean toggle for game flags. With a `label` it lays out a full-width labelled row (44px min).

```jsx
<Switch label="Inébranlable" checked={unbreakable} onChange={setUnbreakable} />
<Switch checked={charge} onChange={setCharge} />
```
