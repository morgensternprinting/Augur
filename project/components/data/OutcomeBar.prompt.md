Stacked single-bar view of the 5 engagement outcomes (win → FUIR). The catastrophe segment glows.

```jsx
<OutcomeBar dist={{ win:0.46, draw:0.10, give:0.18, fallback:0.12, flee:0.14 }} />
```

Fractions auto-normalise. Pair with `<OutcomeLegend />`. `emphasizeFlee` rings + glows the flee segment.
