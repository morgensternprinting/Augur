Coloured dot + word for unit state. Status is always paired with text (low-light accessibility).

```jsx
<StatusDot status="full" />
<StatusDot status="weak" />
<StatusDot status="fleeing" pulse />
<StatusDot status="destroyed" />
```
