The hero metric — expected net VP swing of an engagement. Signed, colour-coded (green +/red −), with a direction word.

```jsx
<VPMeter value={148} range={[92, 196]} />
<VPMeter value={-92} size="compact" />
```

This is the loudest element on the result screen — keep it the only thing at `--fs-hero`.
