Colour-keyed legend for the 5 outcomes. Pass `dist` to show percentages beside each.

```jsx
<OutcomeLegend dist={dist} columns={1} />
```
Always pair with `<OutcomeBar>` so the bar's colours are decodable.
