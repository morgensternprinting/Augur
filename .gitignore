# Local Claude Code settings (machine-specific, not for the repo)
.claude/settings.local.json

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.tmp
*~

# Local preview server logs (the server script + launch.json ARE tracked)
*.log
